resources:
  pipelines:
    simple_pipeline:
      name: just_pipeline_3_pipeline
      catalog: main
      schema: ${var.schema}
      serverless: true
      libraries:
        - notebook:
            path: ../src/just_pipeline_3_pipeline.ipynb